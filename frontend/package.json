{
  "name": "video-converter-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\"",
    "build": "rm -rf dist && mkdir dist && cp public/index.html dist/index.html && cp public/favicon.ico dist/favicon.ico && esbuild src/app.ts --bundle --outfile=dist/bundle.js --sourcemap"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.26.1",
  "devDependencies": {
    "@types/node": "24.10.3",
    "esbuild": "0.27.1",
    "typescript": "5.9.3"
  }
}
